# frozen_string_literal: true

module KodiClient
  module Types
    module Player
      # enum for in/out zoom
      module Zoom
        extend Extensions::Iterable

        IN = 'in'
        OUT = 'out'
      end
    end
  end
end
